apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sentinel-dev

commonLabels:
  environment: dev

resources: []

# Dev overlay uses Helm chart with dev values
# helm install sentinel-dev ./k8s/charts/sentinel -f k8s/overlays/dev/values.yaml -n sentinel-dev
